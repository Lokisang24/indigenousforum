# --- Database (PostgreSQL) ---
# Use a managed Postgres provider, e.g. Supabase, Neon, or Vercel Postgres.
DATABASE_URL="postgresql://postgres:I4g3rWOQnjt5qgPh@db.xijaeduazbjvpbuyvonk.supabase.co:5432/postgres"

# --- Auth ---
# Any long random string. Generate one with: openssl rand -hex 32
JWT_SECRET="replace-with-a-long-random-secret"

# --- Email (SMTP) ---
# Works with any SMTP provider (SendGrid, Mailgun, Gmail app password, Resend SMTP, etc.)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
SMTP_FROM="Indigenous Forums <no-reply@example.com>"

# Where new-application notifications are sent
ADMIN_NOTIFICATION_EMAIL="admin@example.com"

# --- Certificate text (optional — sensible defaults are already set) ---
CERT_ORG_TITLE="SOTHO INDIGENOUS"
CERT_TITLE="IDENTITY CERTIFICATE"
CERT_CERTIFIED_BY="INDIGENOUS FORUM"
CERT_CONTACT_EMAIL="tshele2356@gmail.com"
CERT_TERRITORY="MODERN DAY SA/LES"
CERT_COORDINATING_CHIEF="A. MOKOTJOMELA"
CERT_SENIOR_CHIEF="R. GUGUSHE"
