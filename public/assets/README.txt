Place any custom certificate background/border image(s) here (e.g.
certificate-background.png) and reference them from lib/pdfGenerator.js if
you want an exact visual match to a branded template, instead of the
programmatically-drawn border currently used.
